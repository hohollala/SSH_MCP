{
  "mcpServers": {
    "ssh-mcp": {
      "command": "node",
      "args": ["E:\\MCPProjects\\SSH_MCP\\dist\\index.js", "--stdio"],
      "env": {
        "SSH_MCP_DEBUG": "true",
        "SSH_MCP_TIMEOUT": "30",
        "SSH_MCP_MAX_CONNECTIONS": "10"
      }
    }
  }
}